FROM nginx:{{ nginx_version }}

COPY ./nginx.conf /etc/nginx/nginx.conf
COPY ./default.conf /etc/nginx/conf.d/default.conf

ADD ./{{ nginx_web_dir_host }}/ {{ nginx_web_dir_container }}

EXPOSE {{ nginx_port_int }}
